# Security

AICI processes release metadata and historical operational evidence. The reference implementation does not require network access.

Do not place secrets, raw user prompts, credentials, or personal data into manifests unless your deployment has an explicit data-protection policy.

For production deployments, add authentication, encryption at rest, retention controls, tenant isolation, signed evidence, and access auditing before handling sensitive telemetry.
